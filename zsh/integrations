eval "$(fzf --zsh)"

# Function to wrap the zoxide cd command
zcd() {
  if [ "$#" -eq 0 ]; then
    z
  else
    z "$@"
  fi

  ascii-commit-graph --full-width --show-todos --show-issues
}

alias cd="zcd"

eval "$(zoxide init --cmd cd zsh)"
eval "$(thefuck --alias)"
eval "$(thefuck --alias fk)"
